services:
  api:
    build: .
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - ./outputs:/app/outputs
      - ./qdrant_db:/app/qdrant_db   # only used when QDRANT_URL is not set (local dev)
